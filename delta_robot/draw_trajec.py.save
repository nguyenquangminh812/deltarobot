from ifk import IKinem
from fk import FKinem  
import numpy as np

def draw_line(cur,tar,n):
	theta_list = np.zeros((n,4)) #chia thanh 10 diem
	for i in range(n):
		cur_tmp=cur
		cur_tmp[0] += i*((tar[0]-cur[0])/n)
cur_tmp[0] += i*((tar[0]-cur[0])/n)  
